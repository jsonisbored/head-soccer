{
  "name": "typescript-ka-example",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "esbuild src/main.ts --outfile=app/main.js --bundle --watch --format=esm",
    "build": "esbuild src/main.ts --outfile=app/main.js --bundle --format=esm"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jsonisbored/typescript-ka-example.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.17.5"
  }
}
